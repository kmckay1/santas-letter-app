// The consent cookie's name and values, written by ConsentBanner. Either value
// means the visitor has answered, so the banner stays hidden; only
// 'marketing' loads the Meta Pixel.
export const CONSENT_COOKIE = 'consent'
export const CONSENT_MARKETING = 'marketing'
export const CONSENT_DECLINED = 'declined'
